/* Crie um programa em Linguagem C que conte os 
dígitos de um determinado número usando recursão.*/
#include <stdio.h>

int count_digitos(int num)
{
    if ()
}
int main(void)
{
    int num;

    printf("Digite um valor inteiro: ");
    scanf("%d", &num);

    printf("O numero %d possui %d digitos!\n", num, count_digitos(num));

    return (0);
}
